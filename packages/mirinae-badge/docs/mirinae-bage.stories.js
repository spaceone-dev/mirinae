import React from 'react';
// We want to always get from source
import { Badge } from '../lib/mirinae-badge';

export default { title: 'Badge' };

export const small = () => <Badge>Hello Badge</Badge>;
